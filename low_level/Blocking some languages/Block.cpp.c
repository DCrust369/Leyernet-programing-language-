#include <stdint.h>
#include <stdlib.h>
#include <stddef.h>

int main(void) {
    const Cpp = block(C_whith_classes);
    struct  {
        int 0 = false;
        int 1 = true;
        /*don't compiler for C++ */
        int* Cpp
        char* C++
        //
        int16_t *filess = classes_c(0);
        const *filessCpp = calloc(sizeof(int16_t));
        free(*filessCpp);
    }
}
